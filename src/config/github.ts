/** Canonical GitHub repository URL for star / source links. */
export const GITHUB_REPO_URL = 'https://github.com/akshatthakur22/MailMyCertficate';

/** Minimum batch size before showing the star prompt (first-time generators). */
export const GITHUB_STAR_PROMPT_MIN_CERTIFICATES = 20;

/** Hide prompt for 30 days after "Maybe Later". */
export const GITHUB_STAR_PROMPT_COOLDOWN_MS = 30 * 24 * 60 * 60 * 1000;
